this project is for hook d3d and get the usage and fps

export HookD3D.dll
used by GameCharactorMaker


also, I need a tool to generate video file, a tool to encoding ? ( how about a simple video streming system )
I need a tool to identify the resource requirement for each stage( capturing, transcoding, encoding[x264 and cuda])
A basic version is to auto run the games and record the game's resource requirement with cmd flag to identify use 
second or fps, log into files and then get the scheduling window